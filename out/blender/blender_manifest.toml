schema_version = "1.0.0"

# PROKLADKA — FBX bridge Blender <-> Maya <-> Houdini <-> UE
id = "prokladka"
version = "1.10.0"
name = "PROKLADKA"
tagline = "FBX bridge between Blender and Maya with naming presets"
maintainer = "Maksim Kovalev"
type = "add-on"

blender_version_min = "4.5.0"

license = [
  "SPDX:GPL-3.0-or-later",
]

tags = ["Import-Export", "Pipeline"]
